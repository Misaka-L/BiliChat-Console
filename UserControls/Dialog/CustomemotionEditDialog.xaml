<UserControl x:Class="BiliChat_Console.UserControls.Dialog.CustomemotionEditDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BiliChat_Console.UserControls.Dialog"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Margin="16">
        <TextBox x:Name="ImageUrl" md:HintAssist.Hint="图片地址" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        <TextBox x:Name="DanmakuCommand" md:HintAssist.Hint="弹幕指令" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Style="{StaticResource MaterialDesignFlatButton}"
                IsDefault="True" Margin="0 8 8 0" Content="确定" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Style="{StaticResource MaterialDesignFlatButton}"
                IsCancel="True" Margin="0 8 8 0" Content="取消" Click="CancelButton_Click"/>
        </StackPanel>
    </StackPanel>
</UserControl>
